{
  "name": "@product-feedback/web",
  "version": "0.0.1",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host localhost --port 3000",
    "generate:mock": "npx msw-auto-mock ../api/api-spec.yml --output ./msw-mock-srever.ts",
    "generate": "nuxt generate",
    "postinstall": "nuxt prepare",
    "preview": "nuxt preview",
    "test:bench": "nr test bench",
    "test": "vitest",
    "typecheck": "nuxt typecheck"
  },
  "peerDependencies": {
    "vue-tsc": "^1.0.9"
  },
  "devDependencies": {
    "@nuxt/test-utils": "3.0.0-rc.13",
    "msw": "^0.47.4",
    "nuxt": "3.0.0-rc.13",
    "playwright": "^1.27.1",
    "vitest": "^0.25.0"
  }
}
